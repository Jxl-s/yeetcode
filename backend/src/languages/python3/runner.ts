export class Python3Runner {
    public addPrefix(s: string) {
        const prefix = ``;
        return prefix + s;
    }

    public addSuffix(s: string) {
        const suffix = ``;
        return s + suffix;
    }
}
